# Created by https://www.toptal.com/developers/gitignore/api/MacOS
# Edit at https://www.toptal.com/developers/gitignore?templates=MacOS

### macOS ###
# General
.DS_Store
.AppleDouble
.LSOverride

# Icon must end with two \r
Icon

# Thumbnails
._*

# Files that might appear in the root of a volume
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent

# Directories potentially created on remote AFP share
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk

### macOS Patch ###
# iCloud generated files
*.icloud

# End of https://www.toptal.com/developers/gitignore/api/MacOS

### Node ###
node_modules/

### SvelteKit ###
.svelte-kit/
build/

### Rust / Tauri ###
src-tauri/target/

### IDE ###
.vscode/
.idea/
*.swp
*.swo
*.bak

### Environment ###
.env
.env.*
!.env.example

### Vellis sidecar (ai-collab Phase 4) ###
# `.vellis/` is the per-project mark / inbox / snapshot store
# (`docs/ai-collab.md` §6). Created when the user opens this repo
# itself in vellis for testing — must not be committed.
.vellis/

### E2E test fixtures ###
# Re-created on every `scripts/e2e.sh` run.
tests/e2e/fixtures/

### Claude Code worktrees ###
.claude/worktrees/

### CI artifacts ###
# Where the release job downloads the built DMG before attaching it to the
# Releases (`.github/workflows/release.yml`). Untracked here it would make the
# working tree look dirty to scripts/publish-snapshot.sh.
dist-dmg/

# 外部ツールの生成物
.serena/
graphify-out/
